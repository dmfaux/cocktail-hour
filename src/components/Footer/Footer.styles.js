import styled from "styled-components";

export const Container = styled.div`
    display: flex;
    flex-direction: row;
    padding: 1rem;
    background-color: var(--highlightBg);
    color: var(--highlightBgText);
    align-items:center;
    justify-content: center;
    font-size: var(--smallFont);
`;